const path = '../images/'

const splashScreen = require(path + 'SplashScreen.png');
const profileBackground = require(path + 'ProfileBackground.png');

export default {
    splashScreen,
    profileBackground,
}